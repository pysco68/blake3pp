# Keep docker build contexts lean. Allowlist style: the toolchain images COPY
# only docker/ (recipes and docker/wrappers/) and tools/build-msan-libcxx.sh,
# while build/ holds gigabytes of configured trees. The devcontainer build
# (context ..) COPYs nothing.
*
!docker/
!tools/build-msan-libcxx.sh
