#!/bin/sh
# zig-as-cross-compiler wrapper: $ZIG (or zig on PATH) targeting x86_64-linux-musl.
# CMake needs a single argv[0] compiler; the target flag lives here.
exec "${ZIG:-zig}" cc -target x86_64-linux-musl "$@"
