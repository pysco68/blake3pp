#!/bin/sh
# zig-as-cross-compiler wrapper: $ZIG (or zig on PATH) targeting aarch64-linux-musl.
# CMake needs a single argv[0] compiler; the target flag lives here.
exec "${ZIG:-zig}" c++ -target aarch64-linux-musl "$@"
